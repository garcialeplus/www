# docs (website)

The static website source, served via GitHub Pages. Plain HTML/CSS/images (no
build step).

- `*.html` — the pages (`design.html`, `contact.html`, `collaborations.html`,
  the `403`/`404` error pages, …).
- `css/` — stylesheets.
- `images/` — site imagery.
